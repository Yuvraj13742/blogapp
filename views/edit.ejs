<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white p-10">
    <div class="w-full min-h-screen">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <div class="max-w-2xl mx-auto">
            <h1 class="mb-5 text-2xl">Edit Your Post</h1>
            <form action="/update/<%= post._id %>" method="post">
                <textarea class="w-full resize-none bg-transparent border-2 border-zinc-800 rounded-md p-4 mb-2" placeholder="What's on your mind?" name="content" id=""><%= post.content %></textarea>
                <input class="px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-full md:w-40 cursor-pointer" value="Update Post" type="submit">
            </form>
        </div>
    </div>
</body>
</html>
